name: paste_button_example
description: "Demo app for the paste_button plugin"
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.10.0"
  flutter: ">=3.30.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  paste_button:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
